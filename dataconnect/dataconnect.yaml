specVersion: "v1beta"
serviceId: "tripster.ai"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tripster.ai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
